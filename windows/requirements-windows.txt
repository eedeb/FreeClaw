# Extra dependencies for the Windows tray app (windows/tray.py).
#
# Kept out of the top-level requirements.txt on purpose: nothing on Linux
# imports them, and macOS has its own list with its own backend
# (mac/requirements-mac.txt).

# The notification-area icon, its menu, and Windows toast notifications.
pystray

# pystray needs a real image object for the icon; it does not read .ico by
# itself. This is the only reason Pillow is here.
pillow
